version: '3.7'
services:
  redirectomatic:
    image: docker-registry.lskysd.ca/web-external/redirectomatic:latest
    ports:
      - 80
    networks:
      - lssd_redirectomatic
networks:
  lssd_redirectomatic:
    name: lssd_redirectomatic